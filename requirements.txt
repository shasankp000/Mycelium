# Core ML / NLP
torch>=2.0.0
transformers>=4.38.0
sentence-transformers>=2.6.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# Rate-limiting (M6)
slowapi>=0.1.9
limits>=3.6.0

# Config
toml>=0.10.2
pydantic>=2.6.0

# LLM providers
ollama>=0.2.0
requests>=2.31.0

# Sandbox tools (optional but recommended)
# duckduckgo-search is tried first; sandbox falls back to HTML scrape if absent
duckduckgo-search>=6.1.0

# MCP tool server / client
# mcp_tools_server.py runs as an MCP stdio subprocess; mcp_client.py connects to it.
# The mcp package bundles both the server SDK and the client SDK.
mcp>=1.0.0

# Data utils
tqdm>=4.66.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
